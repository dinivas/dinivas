terraform {
  required_providers {
    digitalocean = {
      source  = "digitalocean/digitalocean"
      version = "~> 2.0"
    }
    keycloak = {
      source  = "mrparkers/keycloak"
      version = "3.2.0-rc.0"
    }
  }
}

provider "digitalocean" {
    token = "{{ access_token }}"
}